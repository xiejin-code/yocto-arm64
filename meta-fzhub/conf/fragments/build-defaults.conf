BB_CONF_FRAGMENT_SUMMARY = "Use shared FZHub build directories."
BB_CONF_FRAGMENT_DESCRIPTION = "Keep downloads and shared state outside the generated BitBake setup."

YOCTO_BASE = "${@os.path.normpath("${TOPDIR}/../../..")}"

DL_DIR = "${YOCTO_BASE}/downloads"
SSTATE_DIR = "${YOCTO_BASE}/sstate-cache"
